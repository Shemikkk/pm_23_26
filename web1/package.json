{ 
  "name": "pm_23_26-web1",
  "version": "1.0.0",
  "description": "Gulp automation demo for web1 (pm_23_26)",
  "scripts": {
    "dev": "echo \"web1 local scripts are deprecated. Use 'npm run dev:web1' from the project root instead.\" && exit 1",
    "build": "echo \"web1 local scripts are deprecated. Use 'npm run build:web1' from the project root instead.\" && exit 1"
  },
  "devDependencies": {
    "browser-sync": "^2.27.10",
    "gulp": "^4.0.2",
    "gulp-concat": "^2.6.1",
    "gulp-file-include": "^2.3.0",
    "gulp-cssnano": "^2.1.3",
    "gulp-rename": "^2.0.0",
    "gulp-sass": "^5.1.0",
    "sass": "^1.64.0",
    "gulp-uglify": "^3.0.2",
    "gulp-imagemin": "^7.1.0"
  }
}
